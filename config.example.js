// Application Configuration

window.config = {
	websocket_server: '192.168.1.107:8082',		// IP and Port of NodeJS Websocket Server
	
	lockout_cuelist: '00001', 					// Cuelist ID to lock out tablets when active
	unlock_code: '1234'							// Code to manually unlock a tablet (can be alphanumeric, no character limit)
}